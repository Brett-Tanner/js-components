// Central button which expands out to an options bar
// Unsure how to make that work on desktop, maybe corner button that does the same???

function rudder() {
  const menu = document.createElement("ul");

  return { menu };
}

export { rudder };
